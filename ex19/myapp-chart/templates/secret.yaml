apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-secret
type: Opaque
data:
  # Helm allows us to encode raw strings into base64 dynamically
  username: {{ "admin_user" | b64enc | quote }}
  password: {{ "super_secure_password" | b64enc | quote }}